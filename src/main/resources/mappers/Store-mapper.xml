<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Store">

	<insert id="insertStore" parameterType="com.kedu.dto.StoreDTO">
        INSERT INTO store
        VALUES (store_seq.nextval, #{memberId}, #{category}, #{name}, #{description}, #{seatStatus}, CURRENT_TIMESTAMP, NULL, #{address1}, #{address2}, #{postcode}, #{seatCapacity})
    </insert>
    
    <!-- 특정 사용자의 가게 수 확인 쿼리 -->
    <select id="countStoresByUserId" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM store WHERE member_id = #{userId}
    </select>
    
</mapper>
